/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
      "images.rawpixel.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;